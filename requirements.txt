pywebview>=5.0
qtpy>=2.4
PySide6>=6.6
mss>=9.0
requests>=2.31
Pillow>=10.0
keyboard>=0.13.5
rapidocr_onnxruntime>=1.3

# 端侧(离线)翻译为可选功能,体积较大,按需安装:
#   pip install argostranslate   然后到「设置 → 翻译设置」下载语言包
# 也可以改用本机 Ollama(无需此依赖)
