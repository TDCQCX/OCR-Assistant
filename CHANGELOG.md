# 更新日志 / Changelog

项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。
格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

## [2.4.0] - 2026-09-14

### 新增(新手教程)

- **内置新手教程:气泡文字 + 箭头指引,逐个模式引导**
  - 五种形态各有一套步骤:悬浮窗(7 步)、迷你条(4 步)、翻译模式(6 步)、自由框选(2 步)、设置窗口(2 步)
  - 表现形式:目标控件高亮描边(带脉动)+ 周围压暗、气泡内写明「这一步做什么」、**气泡外一条引导线箭头直指控件中心**(落点带脉动圆环);支持「上一步 / 下一步 / 跳过」与步骤计数
  - 首次进入某个模式时**自动**开始引导(只在真正显示的模式窗口里播放,不会在隐藏窗口里空播);看完或跳过后记为已看,不再打扰
  - 教程期间悬浮窗会**临时暂停洞口穿透**,结束后自动恢复,因此气泡既看得见也点得到;迷你条会临时加高给气泡留位置,结束后恢复原高度
  - 设置页新增「新手教程」分类:可单独重播任意模式的引导、查看快捷键、一键「重置全部教程」
  - 各模式工具栏新增「新手教程」按钮(info 图标),随时可重看

### 优化(内存与启动,实测)

- **端侧模型切档即释放**:切换端侧翻译档位或切回云端时,释放上一档模型(600M 档常驻约 2.4GB,此前与 1.3B 档会叠加)
- **框选窗口用完即销毁**:全屏透明遮罩此前只是隐藏并常驻内存(实测约 +280MB),现在完成后释放渲染进程,下次框选按需重建
- **设置窗口关闭即销毁**:设置页是重页面(打开约 +290MB),关闭后释放渲染进程与内存
- **精简桥接暴露面**(`Api.__dir__`):pywebview 注入 JS API 时会对 js_api 递归扫描对象图,
  此前每个窗口要扫 213 个对象 / 11006 个属性(21ms),现在只暴露 64 个公开方法 → 1 个对象 / 64 个属性
- 综合效果:启动到首屏 **2.1s**、启动内存 **575MB**(此前 630MB;主进程 376MB)

### 修复(界面与性能,实测验证)

- **修复所有窗口页面加载不出来(严重)**:`private_mode=False` 时 pywebview 的 Qt 后端会为**每个窗口**
  创建一个同名的持久化 `QWebEngineProfile`,多个窗口争抢同一份磁盘缓存目录,导致除第一个之外
  所有窗口的页面都加载失败(表现为整窗空白、启动卡顿)。改为无痕配置后 **5 个窗口在 2.1s 内全部就绪**。
  本程序配置都存在 `config.json`,不依赖浏览器级 cookie/localStorage,因此无痕配置无副作用。
- **修复悬浮窗整窗空白(严重)**:`Overlay.jsx` 中 `setStatus` 未从 `app` 解构,
  状态同步 effect 抛 `ReferenceError` 后 React 卸载整棵树,悬浮窗一直是空白窗口。
- **修复模式切换时抛异常**:`set_mode()` 调用了并不存在的 `_restore_default_size()`,
  `AttributeError` 被主线程调度器的 `except` 静默吞掉,导致**模式没有写回配置**、
  迷你条与翻译窗的首次定位逻辑被整段跳过。现已按模式实现,并支持设置页「恢复默认尺寸」。
- **修复迷你条高度漂移且无法还原**:pywebview 默认 `min_size=(200,100)` 会把 78px 的迷你条顶到 100px;
  且被拉伸的高度会写进配置。现在显式放宽下限,并改为**由前端实测内容自然高度后锁定**
  (随字号/主题自适应,不会裁掉第二行的提问输入框),拉伸只改宽度。
- **修复框选窗口在高 DPI 下比屏幕大 2 倍**:`virtual_screen()` 返回物理像素,而 Qt 窗口用逻辑像素;
  200% 缩放下窗口变成屏幕的 2 倍宽高(4 倍面积),全屏透明窗口的内存从约 170MB 涨到约 690MB。
  现已按屏幕缩放比换算。
- **修复框选页收不到后端推送**(潜在问题):框选页 `selector.html` 是独立入口,不经过 `main.jsx` 的 `<App>`,
  没有注册 `window.__ocrEvent`,因此状态/结果/引导事件在该页面全部丢失;事件入口已下沉到 `bridge.js`,所有窗口共享。
- **修复模型设置页「已填 Key 却仍显示未配置」**(严重):平台列表接口出于安全只回传 `ready` 布尔值(不回传 Key 本身),
  而设置页仍在判断 `p.api_key`,导致已配置的平台全部落到「未配置」分组。已改为认 `ready` 字段。

### 优化

- **设置窗口与框选窗口改为按需创建**:每个 WebEngine 窗口都要占一个渲染进程(约 65MB),
  启动时不再创建这两个窗口。实测启动内存 **776MB → 630MB**,启动到首屏 **2.1s**。
- 窗口尺寸按模式独立保存(悬浮窗 `width/height`、迷你条 `miniWidth/miniHeight`、
  翻译窗 `translateWidth/translateHeight`),在某个模式里缩放不再污染其它模式。
- 提问输入框改为全模式共享同一份内容(任一模式输入,其它模式与重启后保持同步)。

### 修复(重要)

- **修复非百炼平台连通性测试一律 404 的问题(严重)**:预设里只有阿里云百炼填写了完整接口地址,
  OpenAI / DeepSeek / 智谱 GLM / Kimi / Ollama 只填写了 Base URL 前缀(`.../v1`、`/api/paas/v4`),
  而客户端直接向该地址 POST,导致 **404**;该问题同时影响连通性测试与实际的识别/答题/翻译调用。
  现在新增 `chat_endpoint()` 统一归一化:Base URL 填 `https://api.deepseek.com`、
  `https://api.deepseek.com/v1` 或完整接口地址都能正确请求;自建网关/Ollama 原生 `/api/chat` 原样使用。
- 错误提示更明确:404 会直接提示"Base URL 或模型 ID 不正确"并给出实际请求地址;
  401/403 提示 API Key 无效或无权限;400 提示模型 ID 或参数问题。


### 新增

- **端侧翻译新增均衡 / 全量档(官方 NLLB 模型)**:接入 `facebook/nllb-200-distilled-600M`(均衡,约 2.4GB)
  与 `1.3B`(全量,约 5.2GB);模型与分词器均取自官方仓库,不存在第三方转换产物的词表错配问题
- 官方模型运行时按需下载(transformers 等约 48MB,复用本机 torch),不随应用分发
- 档位面板与设置页列出三档(体积 / 实测速度 / 准确度 / 代价 / 语言覆盖),并提供对应运行时下载入口

### 变更

- **许可证由 MIT 变更为 CC BY-NC 4.0(署名—非商业性使用 4.0 国际)**:
  禁止任何商业使用;转载、镜像、二次分发与衍生作品必须标注原作者与项目名、给出原始仓库地址,
  并保留协议全文;个人学习、研究、教学等非商业用途可自由使用与修改
- 许可变更与端侧模型许可保持一致:均衡/全量档使用的 NLLB-200 模型本身即为 CC BY-NC 4.0

### 实测数据(纯 CPU)

- 均衡档:`Hello world. This is a local translation test.` → `这是本地翻译测试.`(首次 11.8s 含加载 / 3.5s)
- 均衡档:`请给出该题目的答案,并解释你的推理过程。` → `Please provide an answer to the question and explain your reasoning process.`(2.9s)
- OCR 三档:轻量 16MB / 0.84s / 字符覆盖 98%;均衡 120MB / 6.5s / 99%;全量 196MB / 4.5s / 97%

### 修复

- 修复端侧模型档位状态误报(函数重复定义导致"已下载却显示未下载")
- 修复官方模型缺少 `tokenizer.json` 导致语言标记无法识别(表现为译文等于原文或退化为重复词)
- 修复镜像 API 限流时下载探测误判为"模型不存在"(改为文件级探测)

## [2.3.0] - 2026-08-24

### 新增

- **端侧模型按需下载**(不再随应用分发):首次把「识别」或「翻译」切到端侧时弹窗询问是否下载,下载中显示进度,下载完成后自动切换到端侧;再次点击若仍未检测到模型会继续提示
  - OCR 识别模型:约 16 MB(RapidOCR PP-OCRv4,取自 PyPI)
  - 推理运行时:约 62 MB(CTranslate2 + sentencepiece,只需一次)
  - 翻译模型:约 79 MB/语言对(Argos CTranslate2 int8 模型 + 分词模型)
- 设置 → 翻译设置新增**端侧模型管理**:查看三类模型状态与占用、一键下载/删除,并显示模型目录

### 优化

- **端侧翻译改用 CTranslate2 小模型**(实测 3 段 0.32s,译文质量可用);此前的 HuggingFace ONNX 方案因词表空间(65k 与 32k spm)不匹配会产生乱码,已弃用
- 翻译模式窗口改为**不透明且默认不置顶**,避免遮挡待翻译内容
- 双语对照改为「原文段落 → 下方译文」,段落之间使用 1px 细分隔线,去掉内部圆角卡片
- 悬浮提示改为**白底黑字**,并自动夹取在窗口内部;窗口过小(如迷你条)时不再使用悬浮提示,改为就地展开文字
- 云端/本地开关文字精简为「云端 / 本地」,尺寸缩小并加入平滑过渡动画
- 悬浮窗顶栏移除「识别」文字与模型下拉框,改为「当前模型 + 状态点」,点击直接进入设置
- 窗口缩放阈值联动模式:悬浮窗小于 420×300 自动切换迷你条,迷你条宽于 560 自动切回悬浮窗;窗口变小时自动隐藏状态标签、结果区与次要按钮
- 打包版不再内置 OCR 模型与端侧推理依赖,exe 体积更小

## [2.2.0] - 2026-08-24

### 新增

- **翻译模式**(第 4 种模式):无洞口、识别与译文区放大的独立窗口;`Ctrl+1/2/3` 之外可从模式切换器直接进入
- **双语逐行/逐段对照**:识别原文按句/段切分后逐段翻译,原文一段、译文紧接其下显示;可切换「双语对照 / 仅译文 / 仅原文」
- **语言方向选择** `[来源语言] ⇄ [目标语言]`(14 种语言),选择结果会写入请求提示词,真正影响翻译方向
- **端侧(离线)翻译模型**:接入 Argos 端侧翻译模型(需 `pip install argostranslate` + 语言包),并支持本机 Ollama 作为备选;设置页可检测可用性、一键下载语言包
- **自动刷新翻译**:定时重新捕获同一区域并翻译,适合字幕/连续内容
- **提问示例与自动记忆**:提问框改为多行大输入框,内置「请回答 / 请翻译 / 请解释 / 请总结 / 请搜索关联内容」等示例下拉,自输入内容自动保存(最近 20 条)
- **主界面引擎开关**:识别 云端/本地 开关直接放在悬浮窗工具栏;翻译模式下另有 识别 与 翻译 两个开关
- **迷你条第二行**:迷你条新增提问输入行,可用图标行 + 提问行直接操作
- **自由截图内嵌控件**:框选后的操作条内置 云端/本地 开关、语言方向、提问输入,并新增「翻译此区域」
- **窗口拖边缩放**:无边框窗口支持四边/四角拖拽改变大小,位置与尺寸自动记忆

### 优化

- 悬浮提示改为自绘样式(修复系统 title 提示黑底、文字不可见的问题)
- 模式切换按钮改为「选中项在图标旁展开文字说明」,悬浮窗与迷你条同步;设置图标改为齿轮
- 本地 OCR 增加同一视觉行合并,避免把一句话切成逐词片段
- 提示词按用户指令类型处理(回答/翻译/解释/总结/搜索),翻译提示词改为按段 JSON 输出
- 翻译模式「设为悬浮窗区域」按洞口尺寸收敛换算窗口尺寸,洞口精确等于框选大小

## [2.1.0] - 2026-08-24

### 修复

- **修复点击切换模式导致程序卡死/崩溃**:前端事件推送此前通过 Qt 主线程调用 `evaluate_js`,而该调用会阻塞等待 JS 返回结果,造成主线程自锁;现改为独立线程串行推送,模式切换、状态与结果推送均不再阻塞界面
- **修复悬浮窗"粘住鼠标"**:改用受控拖拽(Pointer Capture,`pointerup` 必定结束),点击后窗口不再一直跟随鼠标,松手即固定位置
- 连通性测试改为后台线程执行,测试期间界面不再卡顿
- 自由截图取消后正确返回进入前的模式(迷你条 / 悬浮窗)
- 模式重复点击不再重复隐藏/显示窗口,减少闪烁

### 新增

- **悬浮窗 OCR 区域鼠标穿透**:洞口区域从窗口输入区域中挖除(Windows 窗口区域),洞内可直接点击后方内容,同时洞口边框仍保持可见
- 迷你条重做为纯图标工具条:单字母 `O` 标识 + 图标按钮 + 悬停文字提示,首次进入自动居中停靠在**系统任务栏上方**并支持自由拖动
- 新增分区色 token(`panel` / `card` / `sub` / `hover`),面板、卡片、内嵌控件三层对比统一

### 优化

- 清除界面内全部 emoji,统一替换为扁平 SVG 图标(模式切换、设置、置顶、识别、复制、清空、平台、主题、历史、导入导出等)
- 悬浮窗顶部改为图标工具栏(高度 40px),控件高度与字号统一,为洞口让出更多空间
- 设置页统一卡片标题图标、内嵌区配色与控件高度/间距,消除"卡片颜色不一致"
- 自定义主题自动推导缺失的分区色,面板不透明度统一由主题系统处理

## [2.0.0] - 2026-08-24

### 重大变更

- **界面全面重构**:由 PySide6 改为 **React 18 + Tailwind CSS** 前端 + **pywebview(Qt/WebEngine)** 桌面容器,Python 逻辑层完全保留
- 架构分离:前端(`frontend/`,Vite 构建产物输出到 `app/webui/`)+ 后端(`app/`:截图 / OCR / 模型 / 配置 / 历史)

### 新增

- **多模式切换机制**(界面切换器 + `Ctrl+1/2/3`):
  - **悬浮窗模式**:透明洞口常驻置顶,一键识别
  - **自由截图模式**:全屏遮罩拖框选区,支持「立即识别」或「设为悬浮窗区域」(`Ctrl+Shift+A`)
  - **迷你条模式**:极简工具条不遮挡画面,一键框选识别
- **全局主题 + 自定义主题**:内置浅色/深色/灰色/护眼/高对比 5 套预设,支持逐项配色自定义(11 个色值)、圆角、字号、面板不透明度、洞口边框颜色/样式;主题可**导出/导入 JSON**,所有窗口实时同步
- **识别历史**:记录最近 100 条识别结果,可回看、复制、清空
- **请求记录可视化**:GitHub 风格灰→绿 5 级方格图(最近 20 周)
- **更多自定义设置**:图片压缩上限、默认提问、快捷键自定义(识别/自由截图/退出)、缓存与题目保存位置、回答自动入库、请求模板在线编辑与校验
- 平台管理增强:悬浮删除(带确认)、自定义平台、可编辑平台名称、每平台独立的思考开关与 JSON 请求预览

### 优化

- 悬浮窗改为内容驱动布局:折叠识别结果/回答时窗口高度跟随,洞口大小保持不变
- 卡片化 + 圆角化界面,统一配色与对齐;选项卡片等宽居中
- 配置由前端统一管理,关闭窗口自动保存并广播到所有窗口

### 修复

- 修复多窗口(悬浮窗 / 迷你条 / 设置 / 选区)在子线程操作 Qt 导致的卡死:新增主线程调度层 `app/mainthread.py`
- 修复 pywebview 在 WebView2 后端下的 pythonnet 兼容问题(改用 Qt 后端)
- 修复冻结(PyInstaller)模式下配置/缓存路径落到临时目录、无法持久化的问题

### 依赖变更

- 新增 `pywebview`、`qtpy`(PySide6 作为 pywebview 的 Qt 后端);前端使用 React + Tailwind + Vite

## [1.0.0] - 2026-08-24

### 新增

- **透明洞口悬浮窗**:仅洞口区域透明透看目标,其余为不透明面板;拖动定位、边缘缩放,窗口尺寸自动记忆;识别结果/回答区可折叠,折叠时窗口高度跟随、洞口大小不变
- **双 OCR 模式**:
  - 云端:调用所选大模型识别(精度更高,消耗 token)
  - 本地:内置 RapidOCR(ONNX Runtime,PP-OCR 模型,约 50MB,离线免费,CPU 100-300ms)
- **多 AI 平台**:阿里云百炼 / OpenAI / DeepSeek / 智谱 GLM / Kimi / Ollama 一键切换,支持自定义平台(可编辑名称/logo、悬浮删除、确认后移除);未配置平台以暗色区分
- **图文答题**:OCR 文字与截图图片同时发送给模型,结合题目结构化解析(题型/题目/选项);思考模式可关闭以提速
- **本地知识库**:关键词命中即返回答案,免 API 调用;AI 回答可自动入库
- **可编辑 JSON 请求模板**:请求体模板驱动,实时预览,支持自定义模型参数
- **多主题**:黑/白/灰三色预设 + 图片背景,实时预览
- **全局快捷键**:`Ctrl+F1` 截图识别、`Ctrl+Q` 退出(可自定义)
- **配置界面**:左中右导航 + 圆角卡片;模型设置/常规设置/关于应用分页;关闭窗口自动保存
- **请求记录**:GitHub 风格灰→绿 5 级方格图,展示最近 20 周请求情况
- **可编辑平台名称**:点击即编辑,失焦/回车自动提交
- **测试连通性**:绿→灰"测试中"→红/绿状态,成功时显示响应耗时(颜色随耗时变化)

### 优化

- 界面整体采用圆角卡片 + 白灰主色,符合主题色
- 回复区三段独立展示(识别结果/类型与耗时/回答),避免 OCR 与题目重复
- 设置卡片内选项卡片嵌套,字段标签统一对齐

### 修复

- 置顶切换导致窗口消失/闪退(改用 Win32 SetWindowPos,避免重建原生窗口)
- 配置对话框加载字段时回写清空配置(加载期间屏蔽信号)
- 退出重进后状态栏"Key 未配置"不刷新(窗口显示时重新读取持久化配置)
- 折叠识别结果/回答后窗口不跟随、出现空白间距(内容驱动 + 洞口高度钉住)
- PyInstaller 冻结模式下配置/缓存路径落到临时目录导致无法持久化

### 安全

- 打包/仓库初始不携带任何 API Key,敏感信息仅存本地 `config.json`(已加入 `.gitignore`)

### 依赖

- Python 3.10+,PySide6、mss、requests、Pillow、keyboard、rapidocr_onnxruntime

### 致谢

- 题目结构化解析逻辑参考 [AutoAnswer_ai](https://github.com/notxcm/AutoAnswer_ai)
