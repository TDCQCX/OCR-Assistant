# 更新日志 / Changelog

项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。
格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

## [2.0.0] - 2026-08-24

### 重大变更

- **界面全面重构**:由 PySide6 改为 **React 18 + Tailwind CSS** 前端 + **pywebview(Qt/WebEngine)** 桌面容器,Python 逻辑层完全保留
- 架构分离:前端(`frontend/`,Vite 构建产物输出到 `app/webui/`)+ 后端(`app/`:截图 / OCR / 模型 / 配置 / 历史)

### 新增

- **多模式切换机制**(界面切换器 + `Ctrl+1/2/3`):
  - **悬浮窗模式**:透明洞口常驻置顶,一键识别
  - **自由截图模式**:全屏遮罩拖框选区,支持「立即识别」或「设为悬浮窗区域」(`Ctrl+Shift+A`)
  - **迷你条模式**:极简工具条不遮挡画面,一键框选识别
- **全局主题 + 自定义主题**:内置浅色/深色/灰色/护眼/高对比 5 套预设,支持逐项配色自定义(11 个色值)、圆角、字号、面板不透明度、洞口边框颜色/样式;主题可**导出/导入 JSON**,所有窗口实时同步
- **识别历史**:记录最近 100 条识别结果,可回看、复制、清空
- **请求记录可视化**:GitHub 风格灰→绿 5 级方格图(最近 20 周)
- **更多自定义设置**:图片压缩上限、默认提问、快捷键自定义(识别/自由截图/退出)、缓存与题目保存位置、回答自动入库、请求模板在线编辑与校验
- 平台管理增强:悬浮删除(带确认)、自定义平台、可编辑平台名称、每平台独立的思考开关与 JSON 请求预览

### 优化

- 悬浮窗改为内容驱动布局:折叠识别结果/回答时窗口高度跟随,洞口大小保持不变
- 卡片化 + 圆角化界面,统一配色与对齐;选项卡片等宽居中
- 配置由前端统一管理,关闭窗口自动保存并广播到所有窗口

### 修复

- 修复多窗口(悬浮窗 / 迷你条 / 设置 / 选区)在子线程操作 Qt 导致的卡死:新增主线程调度层 `app/mainthread.py`
- 修复 pywebview 在 WebView2 后端下的 pythonnet 兼容问题(改用 Qt 后端)
- 修复冻结(PyInstaller)模式下配置/缓存路径落到临时目录、无法持久化的问题

### 依赖变更

- 新增 `pywebview`、`qtpy`(PySide6 作为 pywebview 的 Qt 后端);前端使用 React + Tailwind + Vite

## [1.0.0] - 2026-08-24

### 新增

- **透明洞口悬浮窗**:仅洞口区域透明透看目标,其余为不透明面板;拖动定位、边缘缩放,窗口尺寸自动记忆;识别结果/回答区可折叠,折叠时窗口高度跟随、洞口大小不变
- **双 OCR 模式**:
  - 云端:调用所选大模型识别(精度更高,消耗 token)
  - 本地:内置 RapidOCR(ONNX Runtime,PP-OCR 模型,约 50MB,离线免费,CPU 100-300ms)
- **多 AI 平台**:阿里云百炼 / OpenAI / DeepSeek / 智谱 GLM / Kimi / Ollama 一键切换,支持自定义平台(可编辑名称/logo、悬浮删除、确认后移除);未配置平台以暗色区分
- **图文答题**:OCR 文字与截图图片同时发送给模型,结合题目结构化解析(题型/题目/选项);思考模式可关闭以提速
- **本地知识库**:关键词命中即返回答案,免 API 调用;AI 回答可自动入库
- **可编辑 JSON 请求模板**:请求体模板驱动,实时预览,支持自定义模型参数
- **多主题**:黑/白/灰三色预设 + 图片背景,实时预览
- **全局快捷键**:`Ctrl+F1` 截图识别、`Ctrl+Q` 退出(可自定义)
- **配置界面**:左中右导航 + 圆角卡片;模型设置/常规设置/关于应用分页;关闭窗口自动保存
- **请求记录**:GitHub 风格灰→绿 5 级方格图,展示最近 20 周请求情况
- **可编辑平台名称**:点击即编辑,失焦/回车自动提交
- **测试连通性**:绿→灰"测试中"→红/绿状态,成功时显示响应耗时(颜色随耗时变化)

### 优化

- 界面整体采用圆角卡片 + 白灰主色,符合主题色
- 回复区三段独立展示(识别结果/类型与耗时/回答),避免 OCR 与题目重复
- 设置卡片内选项卡片嵌套,字段标签统一对齐

### 修复

- 置顶切换导致窗口消失/闪退(改用 Win32 SetWindowPos,避免重建原生窗口)
- 配置对话框加载字段时回写清空配置(加载期间屏蔽信号)
- 退出重进后状态栏"Key 未配置"不刷新(窗口显示时重新读取持久化配置)
- 折叠识别结果/回答后窗口不跟随、出现空白间距(内容驱动 + 洞口高度钉住)
- PyInstaller 冻结模式下配置/缓存路径落到临时目录导致无法持久化

### 安全

- 打包/仓库初始不携带任何 API Key,敏感信息仅存本地 `config.json`(已加入 `.gitignore`)

### 依赖

- Python 3.10+,PySide6、mss、requests、Pillow、keyboard、rapidocr_onnxruntime

### 致谢

- 题目结构化解析逻辑参考 [AutoAnswer_ai](https://github.com/notxcm/AutoAnswer_ai)
