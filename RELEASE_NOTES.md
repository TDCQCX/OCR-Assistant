# OCR 助手 v2.4.0 更新说明

> 距离 v1.0.0(首个发布版)的一次大版本迭代。v1.0.0 只有"透明洞口悬浮窗 + 云端/本地 OCR + 图文答题"这一条主线;
> v2.x 把它扩展成了 **四种模式 + 端侧模型分档 + 全局主题系统** 的完整桌面工具,并修复了若干会导致卡死/结果错误的问题。
> 建议所有 v1.0.0 用户升级。

---

## 一、重大变更

- **界面完全重构**:由 PySide6 改为 **React 18 + Tailwind CSS** 前端 + **pywebview(Qt/WebEngine)** 容器,后端逻辑(截图 / OCR / 模型调用 / 配置)保留并重写调度层。
- **许可证由 MIT 变更为 [CC BY-NC 4.0(署名—非商业性使用)](../../blob/master/LICENSE)**:
  - **禁止任何商业使用**(出售、付费分发、商业产品集成、广告引流等);商业授权请联系作者;
  - **转载 / 镜像 / 二次分发 / 衍生作品必须标明出处**:标注原作者与项目名、给出原始仓库地址、保留协议全文,修改过的须说明"基于原项目修改"。
  - 说明:该协议不属于 OSI 认可的开源许可。端侧翻译「均衡/全量」档使用的 NLLB-200 模型本身即为 CC BY-NC 4.0,与本协议一致。

## 二、新增功能

**0)新手教程(气泡 + 箭头指引,分模式引导)**

| 模式 | 引导内容 |
|---|---|
| 悬浮窗 | 7 步:模式切换 → 洞口 → 提问/指令 → 识别 → 云端/本地 → 当前模型 → 结果区 |
| 迷你条 | 4 步:拖动 → 输入要求 → 一键框选识别 → 切回悬浮窗 |
| 翻译模式 | 6 步:语言方向 → 框选并翻译 → 复用上次区域 → 展示方式 → 附加要求 → 云端/本地 |
| 自由框选 | 2 步:拖拽框选 → 松开后的三个动作 |
| 设置窗口 | 2 步:分类导航 → 保存方式与教程入口 |

- 目标控件**高亮描边 + 周围压暗**,气泡里写清"这一步做什么",**箭头**指向该控件;支持 上一步/下一步/跳过 与步骤计数。
- **首次进入某个模式自动开始**(只在真正显示的那个窗口里播放);看完或跳过后不再打扰,设置页可随时**重播**或**重置全部教程**。
- 教程期间悬浮窗会**临时暂停洞口穿透**、迷你条会临时加高,保证气泡看得见也点得到,结束后自动还原。
- 各模式工具栏新增「新手教程」按钮(info 图标);设置页新增「新手教程」分类(重播、快捷键一览、三步上手)。

**1)四种运行模式(可一键切换,快捷键 `Ctrl+1/2/3`)**

| 模式 | 说明 |
|---|---|
| 悬浮窗模式 | 透明洞口常驻置顶;**洞内鼠标可穿透**,可直接点击后方内容(一边看题一边作答) |
| 翻译模式 | 无洞口窗口,识别区与译文区放大;双语对照 / 仅译文切换;语言方向可选并注入请求 |
| 自由截图模式 | 全屏遮罩拖拽框选;操作条内嵌 识别/翻译 云端-本地开关、语言方向、提问输入,可「识别 / 翻译此区域 / 设为悬浮窗区域」 |
| 迷你条模式 | 图标工具条 + 提问输入行;首次自动居中停靠在任务栏上方,可自由拖动 |

**1.5)平台选择(本轮新增)**:设置 → 模型设置里,**已配置的平台右侧有圆点**,点一下即切换"当前使用"(单选);
未配置的平台没有圆点、不可选。只配一个时自动选中;当前平台被清空 Key 或被删除时自动回落到其它已配置平台;
切换后悬浮窗模型胶囊、状态点、翻译/框选窗口的引擎显示都会同步。缺模型 ID 会标「缺模型」并给出提示。

**2)端侧模型分档(按需下载,不随应用分发)**

| 档位 | OCR 识别 | 端侧翻译 |
|---|---|---|
| 轻量 | PP-OCRv4 mobile,16MB | opus-mt int8,79MB / 语言对 |
| 均衡(默认) | + server 检测模型,120MB | 官方 NLLB-200-600M,约 2.4GB(200 语言单模型) |
| 全量 | + server 识别模型,196MB | 官方 NLLB-200-1.3B,约 5.2GB |

- 首次把「识别」或「翻译」切到本地时会弹窗让用户选择档位并显示**体积 / 实测速度 / 准确度 / 代价**;
- 下载源可选 **自动 / 官方 / hf-mirror**(国内建议镜像);
- 设置页可随时换档、查看占用、删除已下载档位;识别与翻译都切到端侧时**整条流程不需要 API Key**。

**3)其它新增**

- **全局主题 + 自定义主题**:浅色/深色/灰色/护眼/高对比 5 套预设,支持逐项配色、圆角、字号、面板不透明度、洞口边框,主题可导入导出;
- **提问示例与自动记忆**:提问框改为多行大输入框,内置「请回答 / 请翻译 / 请解释 / 请总结 / 请搜索关联内容」等示例,自输入内容自动保存;
- **窗口拖边缩放**:四边/四角可拖动改尺寸;**悬浮窗缩到 420×300 以下自动转迷你条,迷你条拖宽超过 560 自动转回悬浮窗**;
- 识别历史(最近 100 条)、请求记录图(最近 20 周)、连通性测试(异步、含响应耗时)、快捷键自定义、图片压缩上限等设置项。

## 三、优化

- **动画更顺滑(本轮)**:统一缓动与时长;教程高亮不再做「9999px 阴影」关键帧(原来每秒两次全窗口重绘),改为静止阴影 + 只做 transform/opacity 的脉动描边;箭头落点脉动改用 `transform`(不再改 SVG 半径);下载进度条改 `scaleX`;
  拖拽/缩放的指针事件合并到 `requestAnimationFrame`(每帧最多一次跨进程调用),并带 50ms 兜底与"抬起前 flush";拖动期间临时关闭内部元素过渡,避免拖尾;支持系统「减少动态效果」。
- **内存与启动(本轮实测)**:启动到首屏 **2.1s**、启动内存 **575MB**(主进程 376MB + 3 个渲染进程);
  - 端侧翻译**切档即释放**上一档模型(600M 档常驻约 2.4GB,此前会与 1.3B 档叠加),切回云端也会释放;
  - **框选窗口用完即销毁**(此前常驻约 +280MB)、**设置窗口关闭即销毁**(打开约 +290MB),下次按需重建;
  - 精简桥接暴露面(`Api.__dir__`):JS API 注入时的对象图扫描从 213 个对象 / 11006 个属性降到 1 个对象 / 64 个属性;
- 悬浮提示改为**白底黑字**并自动夹取在窗口内;窗口过小时(如迷你条)改为就地展开文字;
- 模式按钮改为**选中项旁展开文字说明**;悬浮窗顶栏只显示「当前模型 + 状态点」;云端/本地开关文字精简为「云端 / 本地」并加过渡动画;
- 本地 OCR 增加**同一视觉行合并**(不再把一句话切成逐词);翻译按**句/段**切分后翻译(避免逐词直译);
- 翻译模式窗口不透明且默认不置顶,避免遮挡待翻译内容;双语为「原文完整显示在上、译文在下」。

## 四、修复(均为用户实际反馈)

| 问题 | 原因与修法 |
|---|---|
| **点击切换模式会卡死/崩溃** | 前端事件推送经 Qt 主线程调用 `evaluate_js`,该调用会阻塞等待 JS 返回导致主线程自锁 → 改为独立线程串行推送 |
| **悬浮窗被鼠标"粘住"** | 弃用内置拖拽,改为受控拖拽(Pointer Capture,`pointerup` 必定结束) |
| 框选翻译后右侧无译文 | 翻译窗页面尚未就绪时结果事件丢失 → 后端保存最近结果,窗口挂载时主动回拉 |
| 译文里混入 `---` 分隔线/提示词 | 翻译提示词改为 JSON 数组进/出,解析层再过滤分隔符与编号残留 |
| 复制译文复制到的是图片 | WebView 内 `navigator.clipboard` 静默失败 → 复制统一走后端系统剪贴板 |
| 打开「翻译设置」整页变空白 | 缺少 `Pill` 导入导致渲染异常 → 修复 |
| 端侧档位"已下载却显示未下载" | 档位状态函数重复定义 → 统一实现 |
| 官方模型翻译输出等于原文/重复词 | 模型目录缺 `tokenizer.json`(语言标记所在)→ 补齐后正常 |
| **迷你条轻微拖动高度剧变且无法还原** | 上下边缘竟可拉伸 + 尺寸写进悬浮窗的键 + 默认最小高度 100px 顶住 → 改为只允许左右拉伸、尺寸写入 `mini_*` 键、高度由内容实测后锁定 |
| **各模式提问输入框内容不同步** | 三个模式是独立网页上下文 → 提问内容改为全局共享并广播到所有窗口,重启后保留 |
| **切换模式后上个模式尺寸被改掉** | 各模式共用一对 `width/height` 键 → 拆成三组独立键,切换时各取自己的尺寸 |
| **启动慢、内存高** | 启动即创建 5 个 WebEngine 窗口(每个约 65MB 渲染进程)+ 每个窗口一个持久化配置目录互抢 + 框选窗口在高 DPI 下被建成屏幕的 2 倍宽高 → 逐项修复,实测见下 |
| **框选页收不到任何后端推送**(潜在) | `selector.html` 是独立入口、不经过主界面的 `<App>`,没有注册事件入口 → 事件入口下沉到 `bridge.js`,所有窗口共享 |
| **模型设置里已填 Key 的平台仍显示"未配置"** | 平台列表接口出于安全只回传 `ready` 布尔值(不回传 Key),设置页却仍读 `api_key` → 改为认 `ready`;实测「已配置(2) 阿里云百炼 / DeepSeek」 |
| **云端翻译 100% 失败** | `app/worker.py` 组装翻译请求时用了 `json.dumps` 却没有 `import json`,直接抛 `name 'json' is not defined`(OCR 已发出、翻译请求发不出去)→ 补上导入,真实云端复测:识别+翻译得到译文 |

### 本轮深度排查额外发现并修复的三个「隐藏」缺陷

> 这些在上一版就已存在,但被静默吞掉或表现为"窗口空白"。

| 问题 | 原因 |
|---|---|
| 所有窗口页面加载不出来(整窗空白) | `private_mode=False` 时 pywebview 为每个窗口创建同名持久化 `QWebEngineProfile`,多窗口争抢同一份磁盘缓存目录 → 改为无痕配置,5 窗口 2.1s 内全部就绪 |
| 悬浮窗整窗空白 | `Overlay.jsx` 里 `setStatus` 未解构,状态 effect 抛 `ReferenceError` 后 React 卸载整棵树 → 修复 |
| 切换模式后配置不写回 | `set_mode()` 调用了不存在的 `_restore_default_size()`,异常被调度器静默吞掉 → 补齐实现 |

## 五、实测数据(纯 CPU,本机)

- **启动到首屏就绪:2.1s**;启动内存 **575MB**(主进程 376MB + 3 个渲染进程各约 65MB);
  关闭设置窗口回收约 **91MB**、用完框选回收约 **62MB**(渲染进程一并释放)
- **新手教程**:五种模式全部实测——气泡/箭头/高亮正常、步骤推进正常、教程中洞口穿透暂停、结束后洞口区域自动恢复、迷你条 94px → 临时 262px → 恢复 94px、看完即记录(重播可用)
- 框选窗口尺寸(200% 缩放):修复前 3200×2000 逻辑像素(屏幕的 2 倍,内存 +690MB)→ 修复后 1600×1000(与屏幕一致,+280MB)
- **OCR 三档**(含小字与表格的截图):轻量 0.84s / 字符覆盖 98%;均衡 6.5s / 99%;全量 4.5s / 97%
- **端侧翻译均衡档(NLLB-600M)**:
  - `Hello world. This is a local translation test.` → `这是本地翻译测试.`(首次 11.8s 含加载,之后 3.5s)
  - `请给出该题目的答案,并解释你的推理过程。` → `Please provide an answer to the question and explain your reasoning process.`(2.9s)
- 全量档(1.3B,约 5.2GB)沿用同一官方推理路径,尚未完成下载实测。

## 六、下载与安装

1. 下载本页附件的 `OCR助手-v2.4.0.exe`(约 288MB,**免安装、绿色运行**);
2. 双击运行,首次启动需解压数秒;若出现 SmartScreen 提示,选择「更多信息 → 仍要运行」(无代码签名,属正常);
3. 打开「设置 → 模型设置」填入你的 API Key 与模型 ID(云端识别/翻译必需);
4. 想完全离线使用:把「识别」「翻译」开关切到本地,按提示选择档位并下载端侧模型即可。

> 配置与模型保存在 exe 同目录(exe 同目录的 `config.json` 与 `models/`),拷贝整个文件夹即可"绿色便携"。

## 七、已知问题与限制

- 端侧翻译在纯 CPU 上较慢(均衡档约 3-4s/段,全量档预计 2-10s/段);日常建议用云端,端侧定位为"离线可用";
- 全量档(5.2GB)尚未实测,如遇问题请反馈;
- 界面手感类问题(洞口穿透范围、迷你条高度、翻译窗默认不置顶)建议实际试用后反馈,我可按你的习惯调整。

## 八、反馈

- GitHub:[https://github.com/TDCQCX/OCR-Assistant](https://github.com/TDCQCX/OCR-Assistant)
- QQ 群:**1108236960**

完整变更记录见 [CHANGELOG.md](../../blob/master/CHANGELOG.md)。
